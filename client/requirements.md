## Packages
(none needed)

## Notes
- Using `framer-motion` (already in dependencies) for premium minimal animations.
- API endpoints `GET /api/messages` and `POST /api/messages` are used to verify fullstack connectivity.
- Tailwind config should theoretically be extended with custom fonts, but CSS variables are used directly for safety.
